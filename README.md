Describe: `pigLatin()`
Test #1: "It will add 'way' to the end of words that begin with a vowel"
Expect(pigLatin("a")).toEqual("away");

Test #2: "It will move all first consecutive consonants to end and add "ay"
Expect(pigLatin("a")).toEqual("ay");

Test #3: "It will add 'way' to the end of words that begin with a vowel"
Expect(pigLatin("a")).toEqual("away");

Describe: `pigLatin()`
Test #4: "It will add 'way' to the end of words that begin with a vowel"
Expect(pigLatin("a")).toEqual("away");

function pigLatin(word) {
  
}